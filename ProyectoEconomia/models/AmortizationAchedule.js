class AmortizationSchedule {
    // Otras funciones y propiedades

    static calcularTabla2(monto, plazo, sistemaAmortizacion) {
        // Implementa la lógica para calcular y generar el contenido de la tabla 2
        let tabla2HTML = '<table>';
        // Agrega filas y celdas a la tabla2HTML según tus cálculos
        // Ejemplo: tabla2HTML += '<tr><td>Cuota 1</td><td>Interés 1</td><td>Capital 1</td></tr>';
        tabla2HTML += '</table>';
        return tabla2HTML;
    }

    // Otras funciones y propiedades
}
